<!-- rounded_grey_edittext_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Use your desired grey color -->
            <corners android:radius="8dp" /> <!-- Adjust the radius to control the roundness of corners -->
        </shape>
    </item>

    <item android:right="1dp" android:left="1dp" android:top="1dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="8dp" />
            <stroke android:color="#808080" android:width="2dp" /> <!-- Adjust the color and width of the stroke -->
        </shape>
    </item>

</layer-list>
